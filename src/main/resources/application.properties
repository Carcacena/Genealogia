propertiesserver.port=${PORT:8080}
server.address=0.0.0.0

# AJUSTADO: Usando os nomes padrão que o Railway injeta nas "8 variables"
#spring.datasource.url=jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}/${MYSQLDATABASE}?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
#spring.datasource.username=root
#spring.datasource.password=${MYSQL_ROOT_PASSWORD}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.datasource.url=jdbc:mysql://${MYSQL_PRIVATE_URL}/${MYSQLDATABASE}?useSSL=true&requireSSL=tru

spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/Gur1j0$e2026!}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:Gur1j0$e2026!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver





spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true



spring.flyway.enabled=false
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

app.jwt.secret=${JWT_SECRET}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}
