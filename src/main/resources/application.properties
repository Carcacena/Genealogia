spring.application.name=arvore-geneologia-pro

spring.datasource.url=jdbc:mysql://${MYSQLHOST:localhost}:${MYSQLPORT:3306}/${MYSQLDATABASE:arvore}?autoReconnect=true&allowPublicKeyRetrieval=true&useSSL=false&connectTimeout=10000&socketTimeout=10000
spring.datasource.username=${MYSQLUSER:root}
spring.datasource.password=${MYSQLPASSWORD:Gur1j0$e2026!}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver



spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true
spring.flyway.locations=classpath:db/migration,classpath:db/testdata

server.port=${PORT:8080}
server.address=0.0.0.0

jwt.secret=${JWT_SECRET:genealogia_local_dev_secret_1234567890_123456}
jwt.expiration=${JWT_EXPIRATION:86400000}

